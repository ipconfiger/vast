Task 17: Channel archive/unarchive with write blocking
=====================================================
Date: 2026-07-01

## Changes Made

### 1. src/ws/protocol.rs
- Added `ChannelUnarchived { channel_id: String }` variant to `ServerEvent` enum.

### 2. src/api/channels.rs
- Added `archive_channel` handler: POST /api/channels/{id}/archive
  - Checks owner_id matches authenticated user
  - Sets is_archived=1 in DB
  - Broadcasts `ChannelArchived` via WebSocket
- Added `unarchive_channel` handler: POST /api/channels/{id}/unarchive
  - Checks owner_id matches authenticated user
  - Sets is_archived=0 in DB
  - Broadcasts `ChannelUnarchived` via WebSocket

### 3. src/api/mod.rs
- Added `post` to routing imports
- Registered `/channels/{id}/archive` and `/channels/{id}/unarchive` routes
- Also added `/channels/{channel_id}/messages` route (was missing from test router)

### 4. src/main.rs
- Registered `/channels/{id}/archive` and `/channels/{id}/unarchive` routes in api_routes()

### 5. send_message already checks is_archived (from T14) — no change needed

## Tests Added (8 new tests, all pass)
- test_archive_channel_success — owner archives, is_archived=true
- test_unarchive_channel_success — owner unarchives, is_archived=false
- test_archive_not_owner — non-owner gets 403
- test_unarchive_not_owner — non-owner gets 403
- test_archive_not_found — nonexistent channel gets 404
- test_unarchive_not_found — nonexistent channel gets 404
- test_archive_requires_auth — no token gets 401
- test_archive_blocks_writes — POST message to archived channel returns 403
- test_archive_allows_reads — GET and list still work after archive

## Test Results
cargo test: 62 passed (1 suite)
